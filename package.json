{
  "name": "rover-finance",
  "version": "2.0.0",
  "description": "Controle de fluxo de caixa pessoal — Rover Finance Web",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap": "capacitor",
    "cap:add:android": "capacitor add android",
    "cap:open:android": "capacitor open android",
    "cap:sync": "capacitor sync android",
    "cap:build": "npm run build && capacitor sync android",
    "android:build": "npm run build && capacitor sync android && capacitor build android"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
